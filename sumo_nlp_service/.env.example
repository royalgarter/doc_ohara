# Port to expose the API on the host
API_PORT=8765

# Ollama model to use (must be pulled on first run — entrypoint handles this automatically)
# gemma4:e2b-it-qat = 4.3GB, fast (default)
# gemma4:12b-it-qat = 7.2GB, better quality
OLLAMA_MODEL=gemma4:e2b-it-qat
